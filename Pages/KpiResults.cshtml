@page
@model EmployeeHealth.Web.Pages.KpiResultsModel
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ผลลัพธ์ KPI - EmployeeHealth</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="min-h-screen bg-gray-50">
  <div class="flex">
    <aside class="w-64 bg-white p-6 border-r">
      <div class="font-bold text-blue-600 mb-6 text-xl">HEALTHPLUS</div>
      <nav class="space-y-2 text-sm">
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/Dashboard">แดชบอร์ด</a>
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/ExerciseLogs">บันทึกออกกำลัง</a>
        <a class="block px-3 py-2 bg-blue-500 text-white rounded shadow" href="/KpiResults">ผลลัพธ์ KPI</a>
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/Profile">โปรไฟล์ของฉัน</a>
      </nav>
    </aside>

    <main class="flex-1 p-8">
      <div class="grid grid-cols-4 gap-6">
        <!-- Header Section -->
        <div class="col-span-4 mb-6">
          <div class="bg-gradient-to-r from-blue-50 to-blue-100 p-6 rounded-lg border border-blue-200">
            <div class="flex justify-between items-start">
              <div>
                <div class="text-sm text-gray-500 mb-1">KPI</div>
                <div class="text-gray-700">สุขภาพการออกกำลัง | EMP-001</div>
              </div>
              <div class="text-right">
                <div class="flex items-center justify-end gap-4">
                  <div class="text-center">
                    <div class="text-2xl font-bold text-blue-600">@(Model.Summary?.SuccessRate.ToString("F1") ?? "0.0")</div>
                    <div class="text-xs text-gray-600">OUT OF 5.0</div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Main Content -->
        <div class="col-span-3">
          <!-- KPI SCORE Card -->
          <div class="p-6 bg-white rounded-lg shadow mb-6">
            <div class="flex items-start gap-4">
              <div class="w-16 h-16 bg-blue-600 rounded-lg flex items-center justify-center text-white text-2xl">
                📌
              </div>
              <div>
                <h2 class="text-2xl font-bold">KPI SCORE 2023</h2>
                <p class="text-sm text-gray-600">Employee Performance Summary</p>
                <div class="flex gap-4 mt-2">
                  <span class="px-3 py-1 bg-blue-100 text-blue-700 text-xs font-semibold rounded">STATUS EXCELLENT</span>
                  <span class="px-3 py-1 bg-gray-100 text-gray-700 text-xs font-semibold rounded">LAST UPDATED TODAY</span>
                </div>
              </div>
            </div>
          </div>

          <!-- Metrics Cards -->
          <div class="grid grid-cols-3 gap-4 mb-6">
            <div class="p-4 bg-white rounded-lg shadow">
              <div class="flex items-center gap-2 mb-2">
                <span class="text-xl">⚡</span>
                <span class="text-sm text-gray-600 font-semibold">TOTAL SESSIONS</span>
              </div>
              <div class="text-3xl font-bold">@(Model.Summary?.ExerciseCount ?? 0)</div>
            </div>
            <div class="p-4 bg-white rounded-lg shadow">
              <div class="flex items-center gap-2 mb-2">
                <span class="text-xl">✓</span>
                <span class="text-sm text-gray-600 font-semibold">QUALIFIED</span>
              </div>
              <div class="text-3xl font-bold">@(Model.Summary?.QualifiedSessions ?? 0)</div>
            </div>
            <div class="p-4 bg-white rounded-lg shadow">
              <div class="flex items-center gap-2 mb-2">
                <span class="text-xl">📈</span>
                <span class="text-sm text-gray-600 font-semibold">SUCCESS RATE</span>
              </div>
              <div class="text-3xl font-bold">@(Model.Summary?.SuccessRate.ToString("F0") ?? "0")%</div>
            </div>
            <div class="p-4 bg-white rounded-lg shadow">
              <div class="flex items-center gap-2 mb-2">
                <span class="text-xl">🏆</span>
                <span class="text-sm text-gray-600 font-semibold">REWARD RANK</span>
              </div>
              <div class="text-3xl font-bold">A+</div>
            </div>
          </div>

          <!-- Monthly Progression -->
          <div class="p-6 bg-white rounded-lg shadow mb-6">
            <h3 class="text-lg font-bold mb-4">MONTHLY PROGRESSION</h3>
            
            @if (Model.MonthlyKpis.Any())
            {
              <div class="overflow-x-auto">
                <table class="w-full text-sm">
                  <thead>
                    <tr class="border-b">
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">ปี</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">เดือน</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">ทั้งหมด</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">ผ่าน</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">อัตราสำเร็จ (%)</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">คะแนน KPI</th>
                      <th class="text-left py-3 px-4 font-semibold text-gray-600">สถานะ</th>
                    </tr>
                  </thead>
                  <tbody>
                    @foreach (var kpi in Model.MonthlyKpis)
                    {
                      <tr class="border-b hover:bg-gray-50">
                        <td class="py-3 px-4">@kpi.Year</td>
                        <td class="py-3 px-4">@kpi.MonthName</td>
                        <td class="py-3 px-4">@kpi.TotalSessions</td>
                        <td class="py-3 px-4">@kpi.QualifiedSessions</td>
                        <td class="py-3 px-4">@kpi.SuccessRate.ToString("F0")%</td>
                        <td class="py-3 px-4 font-semibold">@kpi.KpiScore.ToString("F0")/100</td>
                        <td class="py-3 px-4">
                          @if (kpi.Status == "ผ่าน")
                          {
                            <span class="inline-block px-3 py-1 bg-green-100 text-green-800 rounded-full text-xs font-semibold">ผ่าน</span>
                          }
                          else
                          {
                            <span class="inline-block px-3 py-1 bg-yellow-100 text-yellow-800 rounded-full text-xs font-semibold">ต้องปรับปรุง</span>
                          }
                        </td>
                      </tr>
                    }
                  </tbody>
                </table>
              </div>
            }
            else
            {
              <p class="text-gray-500 text-center py-6">ไม่มีข้อมูล KPI สำหรับเดือนที่ผ่านมา</p>
            }
          </div>
        </div>

        <!-- Sidebar -->
        <aside class="p-6 bg-white rounded-lg shadow h-fit">
          <div class="mb-6">
            <div class="text-sm text-gray-400 mb-3">KPI YEAR 2023</div>
            <div class="text-4xl font-bold text-blue-600">@(Model.Summary?.SuccessRate.ToString("F1") ?? "0.0")</div>
            <div class="h-1 bg-gradient-to-r from-blue-600 to-blue-400 mt-2 rounded"></div>
          </div>

          <div class="p-4 bg-blue-900 text-white rounded-lg">
            <div class="flex items-center gap-2 mb-3">
              <span class="text-lg">ℹ️</span>
                                <span class="font-bold">เกณฑ์การประเมิน</span>
            </div>
            <ol class="space-y-2 text-xs">
              <li><span class="font-bold text-blue-300">1</span> DURATION TARGET - ออกกำลังไม่ต่ำกว่า 30 นาที</li>
              <li><span class="font-bold text-blue-300">2</span> FREQUENCY TARGET - ออกกำลังกาย 3 ครั้งต่อสัปดาห์</li>
            </ol>
          </div>
        </aside>
      </div>
    </main>
  </div>
</div>
</body>
</html>
