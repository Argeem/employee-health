@page
@model EmployeeHealth.Web.Pages.ProfileModel
<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>โปรไฟล์ - EmployeeHealth</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="min-h-screen bg-gray-50">
  <div class="flex">
    <aside class="w-64 bg-white p-6 border-r">
      <div class="font-bold text-blue-600 mb-6 text-xl">HEALTHPLUS</div>
      <nav class="space-y-2 text-sm">
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/Dashboard">แดชบอร์ด</a>
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/ExerciseLogs">บันทึกออกกำลัง</a>
        <a class="block px-3 py-2 text-gray-500 hover:text-gray-700" href="/KpiResults">ผลลัพธ์ KPI</a>
        <a class="block px-3 py-2 bg-blue-500 text-white rounded shadow" href="/Profile">โปรไฟล์ของฉัน</a>
      </nav>
    </aside>

    <main class="flex-1 p-8">
      <div class="grid grid-cols-3 gap-6">
        <div class="col-span-2">
          <div class="p-6 bg-white rounded-lg shadow">
            <h2 class="text-2xl font-bold mb-6">โปรไฟล์</h2>
            
            <div class="flex items-start gap-6">
              <div class="w-32 h-32 bg-blue-600 rounded-lg flex items-center justify-center text-white text-5xl font-bold">
                <span>🏥</span>
              </div>
              
              <div class="flex-1">
                <h3 class="text-2xl font-bold mb-4">@(Model.HealthData?.Name ?? "ไม่พบข้อมูล")</h3>
                
                <div class="grid grid-cols-2 gap-4 mb-6">
                  <div>
                    <p class="text-sm text-gray-500">รหัสพนักงาน</p>
                    <p class="text-lg font-semibold">@(Model.HealthData?.EmpId ?? "N/A")</p>
                  </div>
                  <div>
                    <p class="text-sm text-gray-500">จำนวนครั้งออกกำลัง</p>
                    <p class="text-lg font-semibold">@(Model.HealthData?.ExerciseCount ?? 0) ครั้ง</p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="mt-6 p-6 bg-white rounded-lg shadow">
            <h3 class="text-xl font-bold mb-4">ข้อมูลสุขภาพ</h3>
            
            <div class="grid grid-cols-2 gap-6">
              <div>
                <p class="text-sm text-gray-500 mb-2">น้ำหนักล่าสุด</p>
                <p class="text-2xl font-bold">
                  @if (Model.HealthData?.LatestWeight.HasValue ?? false)
                  {
                    <span>@Model.HealthData.LatestWeight.Value.ToString("F1") กก.</span>
                  }
                  else
                  {
                    <span class="text-gray-400">ยังไม่มีข้อมูล</span>
                  }
                </p>
              </div>
              <div>
                <p class="text-sm text-gray-500 mb-2">จำนวนครั้งออกกำลัง</p>
                <p class="text-2xl font-bold">@(Model.HealthData?.ExerciseCount ?? 0) ครั้ง</p>
              </div>
            </div>
          </div>

          <div class="mt-6 text-right">
            <button class="px-6 py-2 bg-blue-600 text-white rounded-md shadow hover:bg-blue-700">แก้ไขโปรไฟล์</button>
          </div>
        </div>

        <aside class="p-6">
          <div class="p-4 bg-white rounded-lg shadow mb-4">
            <div class="text-sm text-gray-400">สถานะข้อมูล</div>
            @if (Model.HealthData != null)
            {
              <div class="mt-2 text-sm text-green-600">✓ ดึงข้อมูลจากฐานข้อมูลเรียบร้อย</div>
            }
            else
            {
              <div class="mt-2 text-sm text-red-600">✗ ไม่พบข้อมูล</div>
            }
          </div>

          <div class="p-4 bg-white rounded-lg shadow">
            <div class="text-sm text-gray-400">ข้อมูลพื้นฐาน</div>
            <div class="mt-2 text-xs text-gray-600">
              <p>รหัส: @(Model.HealthData?.EmpId ?? "N/A")</p>
              <p>ชื่อ: @(Model.HealthData?.Name ?? "N/A")</p>
              <p>ออกกำลัง: @(Model.HealthData?.ExerciseCount ?? 0) ครั้ง</p>
              <p>น้ำหนัก: @(Model.HealthData?.LatestWeight?.ToString("F1") ?? "N/A") กก.</p>
            </div>
          </div>
        </aside>
      </div>
    </main>
  </div>
</div>
</body>
</html>
